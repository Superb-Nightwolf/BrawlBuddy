[project]
name = "brawl-advisor"
version = "0.1.0"
description = "Brawl Stars account intelligence and progression planning"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"

